# تقرير تدقيق Kernel — المرحلة أ
**التاريخ:** 26 يونيو 2026  
**النطاق:** 11 صفحة في مسار /kernel/  
**الأداة:** تحليل الكود المصدري (Astro) + معاينة حية عبر Playwright

---

## 📋 جدول المشاكل

| # | الصفحة | المشكلة | النوع | الخطورة | الإصلاح المقترح |
|---|--------|---------|------|---------|-----------------|
| 1 | **جميع الصفحات الفرعية** (chat, audit, approvals, stats, connectors, scenarios, policies, evidence, compliance, reports) | عدم اتساق بصري تام مع صفحة الفهرس. الصفحات المهاجرة تستخدم HTML قديم عبر `set:html` بتصميم مختلف كلياً (أزرار، بطاقات، تخطيط) عن `index.astro` الذي يستخدم توكن V2 ومكونات حديثة. | **تصميم** | **حرجة** | توحيد جميع الصفحات تحت نظام تصميم واحد. إما ترحيل كل الصفحات المهاجرة إلى مكونات Astro حديثة، أو جعل `migrated-legacy-content` يتوافق مع توكنات V2. |
| 2 | **جميع الصفحات** | خلط توكنات CSS: `index.astro` و `KernelLayout.astro` يستخدمان توكن V2 (`--ink-*`, `--brand`, `--space-*`, `--text-soft`)، بينما `[slug].astro` و `ActivityStream.astro` يستخدمان توكنات قديمة (`--fg`, `--fg-muted`, `--border`, `--card-bg`). | **تصميم** | **حرجة** | توحيد جميع الملفات على توكن V2 فقط. استبدال `--fg` → `--text`، `--fg-muted` → `--text-soft`، `--border` → `--border` (موجود)، `--card-bg` → `--surface`. |
| 3 | `src/data/kernel.ts` السطر 79 | `kernelUnits` يحتوي وحدة `index` كأول عنصر، لكن `kernelNavItems` يفلترها. `getStaticPaths` في `[slug].astro` يستخدم `kernelNavItems` فقط. هذا صحيح ولا يسبب مشكلة. | **تصميم** | **بسيطة** | لا حاجة للإصلاح حالياً — يعمل كما هو متوقع. |
| 4 | `src/styles/tokens.css` سطر 374 | `--brand-500: var(--brand);` يتسبب في circular reference أو على الأقل يعيد تعريف brand-500 بقيمة brand-400 (#22d3ee) بدلاً من القيمة الأصلية (#06b6d4). | **تصميم** | **متوسطة** | إصلاح التعريف: إما حذف السطر 374، أو تعيين `--brand-500` بالقيمة الصحيحة (#06b6d4). |
| 5 | `src/pages/kernel/[slug].astro` | الصفحات المهاجرة (migrated) تُعرض عبر `<main class="migrated-legacy-content" set:html={migratedPage.html}>` مما يلغي تماماً هيكل `k-page` (breadcrumb، h1، الأقسام النصية، cross-links). | **تصميم / خطأ** | **حرجة** | دمج محتوى JSON المهاجر داخل هيكل `k-page` للحفاظ على التناسق البصري عبر جميع الصفحات الفرعية. |
| 6 | `src/layouts/KernelLayout.astro` | التنقل (sticky topnav + drawer + bottomnav) مطبق مباشرة في الـ layout باستخدام `<style>` — هذا يمنع إعادة الاستخدام. | **تصميم** | **متوسطة** | استخراج التنقل إلى مكون `.astro` منفصل (KernelNav.astro). |
| 7 | **جميع الصفحات المهاجرة** | الـ HTML المهاجر يستخدم ألواناً وتصميماً غير متوافقين مع نظام V2. مثلاً: `#00D4AA` بدل `var(--brand)` أو `#22d3ee`. الخلفيات تستخدم `rgba` مباشرة. | **تصميم** | **حرجة** | تحديث HTML المهاجر لاستخدام توكنات V2. |
| 8 | `src/pages/kernel/[slug].astro` سطر 284 | زر CTA لونه `background: var(--brand, #00D4AA)` — القيمة الاحتياطية (#00D4AA) تختلف عن قيمة brand الحقيقية (#22d3ee). | **تصميم** | **بسيطة** | تغيير القيمة الاحتياطية أو حذفها. |
| 9 | **فهرس Kernel** (`index.astro`) | النص الرمادي (#94a3b8) على الخلفية الداكنة (#111827/#040711) قريب من حد WCAG AA. | **a11y** | **متوسطة** | زيادة تباين النص الرمادي إلى #cbd5e1 أو أفتح. |
| 10 | `ActivityStream.astro` | النصوص تستخدم `--fg-muted` (#94a3b8) للوصف والوقت — تباين منخفض. | **a11y** | **متوسطة** | استخدام `--text-soft` أو `--text-secondary`. |
| 11 | `[slug].astro` السطور 232-239 | Breadcrumb في الصفحات غير المهاجرة يستخدم gap و font-size بقيم ثابتة بدل توكنات V2. | **تصميم** | **بسيطة** | استبدال القيم الثابتة بتوكنات V2. |
| 12 | **جميع الصفحات** | لا توجد روابط متبادلة كافية بين الصفحات الفرعية. فقط روابط لـ: العودة لفهرس Kernel + الحل المقابل + التوثيق + احجز استشارة. ما في روابط مباشرة بين chat → policies → approvals. | **ترابط** | **متوسطة** | إضافة قسم "الصفحات ذات الصلة" لكل صفحة يضم روابط لـ 3-4 صفحات ذات صلة. |
| 13 | `index.astro` | قسم cross-links جيد (حلول الحوكمة، توثيق Kernel، احجز استشارة). الوحدات في `k-units` تغطي الصفحات الفرعية. | **ترابط** | **بسيطة** | لا حاجة — الوحدات موجودة. |
| 14 | **جميع الصفحات المهاجرة** | محتوى SEO طويل (مثل chat عنده 7 فقرات + عدة h2/h3) — ممتاز. لكنه غير موجود في الصفحات غير المهاجرة. | **SEO** | **متوسطة** | إضافة محتوى SEO مماثل للصفحات غير المهاجرة. |
| 15 | **جميع الصفحات** | حالات الخطأ/التحميل/الفارغة غير موجودة في أغلب الصفحات. chat عنده حالة فارغة فقط. | **خطأ / UX** | **متوسطة** | إضافة حالات فارغة + خطأ لكل صفحة. |
| 16 | `index.astro` السطور 278-280 | اختصارات لوحة المفاتيح مخفية وما فيها طريقة لفتحها غير "?" اللي ما يعرفها الزوار. | **UX** | **بسيطة** | إضافة زر صغير لتذكير المستخدم بالاختصارات. |
| 17 | `[slug].astro` | التداخل بين محتوى JSON المهاجر والنص المبرمج في Astro يسبب ارتباك بصري للمستخدم. | **تصميم / UX** | **متوسطة** | توحيد التقديم: إما كل المحتوى من JSON أو كل المحتوى من Astro. |
| 18 | `src/data/kernel.ts` | كل بيانات العرض ثابتة — لا توجد محاكاة لوقت الاستجابة (loading state). | **UX / أداء** | **بسيطة** | إضافة تأخير مصطنع لإظهار حالة التحميل. |
| 19 | **جميع الصفحات** | الأيقونات SVG مضمنة مباشرة في TypeScript (kernel.ts) — يمنع التخزين المؤقت ويزيد حجم الصفحة. | **أداء** | **بسيطة** | استخراج الأيقونات إلى مكون منفصل أو استخدام sprite SVG. |
| 20 | `KernelLayout.astro` | زر القائمة الجانبية معطل في SSR (لا JS إلا بعد astro:page-load) — المستخدمون بلا JS لا يقدرون فتح القائمة. | **a11y / أداء** | **متوسطة** | إضافة `<noscript>` fallback أو استخدام `<details>/<summary>`. |

---

## 🟢 ملخص الخطورة

| الخطورة | العدد |
|---------|-------|
| **حرجة** | 4 |
| **متوسطة** | 11 |
| **بسيطة** | 5 |

---

## 🏗️ ملاحظات إضافية

### نقاط القوة
- بنية `KernelLayout` ممتازة: skip-link, تنقل متجاوب, RTL صحيح.
- صفحة الفهرس (`index.astro`) متكاملة ومحتوى غني.
- IntersectionObserver خفيف ولا يحتاج GSAP.
- اختصارات لوحة المفاتيح ممتازة.
- كل الصفحات عندها `aria-current`, `aria-label`, `role` مناسب.

### توصيات المرحلة ب
1. **توحيد نظام التصميم**: مكونات مشتركة — `KernelCard`, `KernelBadge`, `KernelTable`, `KernelStatus`.
2. **ترحيل المحتوى المهاجر**: تحويل JSON → مكونات Astro حقيقية.
3. **إعادة هيكلة CSS**: نقل CSS الخاص بـ Kernel إلى ملف مركزي `src/styles/kernel.css`.
4. **تحسين الترابط**: إضافة `relatedSlugs` لكل وحدة في `kernel.ts`.
5. **حالات UX**: لكل مكون، إضافة `loading`, `empty`, `error`.

---

*انتهى تحليل المرحلة أ. في انتظار موافقتك للمتابعة إلى المرحلة ب.*
