---
/**
 * KernelActivityList — Activity stream with icon + title + meta + time.
 * Items passed in as slot — pages compose freely.
 */
---

<ul class="kernel-activity-list">
  <slot />
</ul>

<style is:global>
  .kernel-activity-list { /* global so child pages can use without re-importing */ }
</style>