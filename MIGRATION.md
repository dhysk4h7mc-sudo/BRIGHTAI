# ملاحظات الانتقال

لا توجد تغييرات كاسرة مقصودة في الروابط أو مسارات الصفحة الرئيسية.

## ما تغيّر

- الصفحة تعتمد الآن على ملفات CSS جديدة داخل `assets/css`.
- المحادثة تعتمد على `assets/js/main.bundle.min.js` بدلاً من سكربت داخلي داخل الصفحة.
- بريد التواصل في البيانات المنظمة والواجهة أصبح `info@brightai.site`.

## ما يجب الانتباه له

- إذا عدّلت تفاعلات المحادثة، عدّل المصدر `assets/js/main.bundle.js` ثم أعد بناء النسخة المصغرة.
- إذا عدّلت تصميم الصفحة الرئيسية، استخدم `home-design-tokens.css` بدلاً من تعديل `design-tokens.css` العام إلا إذا كان التغيير عاماً لكل الموقع.
