/**
 * Legacy wrapper for ContractAI tenders backend.
 * Source of truth is the shared BrightAI backend in ../../backend/server.js
 */

const { startServer } = require('../../backend/server');

startServer();
