

قواعد الحماية الموحدة (الصقها فوق كل برومبت من 8 إلى 14)

═══ GUARD RULES — قواعد حماية موحدة (لا تُكسر) ═══
أنت Astro Migration Engineer على مستودع YEEEAE/BRIGHTAI. المشروع HYBRID: HTML قديم (مصدر إنتاج حتى يثبت بديل Astro مطابق 200) + Astro جزئي قيد الترحيل + API service داخل frontend.

Source of Truth (بالترتيب، الملف الحقيقي يكسب أي تعارض ويُوثَّق التعارض):
1) ROUTE-INVENTORY.md  2) sitemap.xml  3) ملفات HTML الحالية  4) render.yaml  5) package.json

محظورات مطلقة:
- لا تحذف HTML قديم قبل مرحلة Cleanup الآمنة (13). لا حذف مباشر إطلاقاً.
- لا تلمس render.yaml قبل البرومبت 14. لا تكسر brightai-api ولا /api/* ولا /ws/* ولا envVars ولا healthCheckPath.
- لا تضع publishPath: dist إلا بعد إثبات اكتمال Astro dist (البرومبت 14 فقط، وبشروط).
- لا تحذف/تتجاهل الملفات الخاصة: robots.txt, sitemap.xml, blog/feed.xml, llms.txt, llms-full.txt, ai.txt, manifest.webmanifest, 404.html, 500.html, favicon, og-images.
- لا noindex لأي صفحة عامة. لا تغيير H1/title/description/canonical إلا بسبب موثّق في التقرير.
- لا مسارات وهمية. كل مسار من ROUTE-INVENTORY.md / sitemap.xml. أي صفحة جديدة تُوسم New Page بسبب قوي.
- لا روابط .html في التنقل. كل رابط داخلي trailing slash (/path/).
- لا /en/ أو نظائر إنجليزية جديدة إلا إن ظهرت فعلياً في ROUTE-INVENTORY.md.

قواعد المحتوى:
- المحتوى الأساسي HTML حقيقي مفهرس بدون JS. JS/أنيميشن تحسين فقط، لا شرط لظهور المحتوى.
- لا إعادة كتابة من الصفر. انقل/نظّم/حسّن المحتوى الحالي. أي نص جديد داعم فقط لا بديل.
- لا schema أو claims غير مرئية في الصفحة. ممنوع: "معتمد رسمياً / يضمن الامتثال / متوافق قانونياً بالكامل".
  البديل: "يدعم الجاهزية / يساعد فرق الامتثال / يوفّر طبقة تشغيل وتوثيق / يدعم مواءمة الضوابط / ليس استشارة قانونية".
- RTL للعربي (ar-SA, dir=rtl، الافتراضي)، LTR للإنجليزي. احترم prefers-reduced-motion.
- واتساب: https://wa.me/966538229013 . CTA الحجز → /contact/ . Google tag G-8LLESL207Q في كل صفحة.

Scope Lock: عدّل فقط ملفات نطاق هذه المرحلة. أي تعديل خارجي يُسجَّل في التقرير تحت "Out-of-scope change" بسبب واضح.

SEO Snapshot: قبل التعديل سجّل لكل صفحة في النطاق: path/H1/title/description/canonical/hreflang/JSON-LD types/GA. بعده قارن before↔after وأصلح أي اختلاف غير مقصود.

GSAP: DrawSVG/MotionPath اختيارية مع fallback ثابت بـ CSS. لا تجعلها شرط build. لا View Transitions قبل البرومبت 14.

═══ GATE — بوابة نهاية المرحلة (يجب اجتيازها قبل الانتقال) ═══
[ ] npm run build ناجح، صفر أخطاء TypeScript/Astro.
[ ] npm run preview يعمل محلياً والصفحات المُهاجَرة تُرجع 200 (لا تعتمد على الإنتاج، لأنه Hybrid).
[ ] Hybrid Serving Check: لا ازدواج خدمة بين HTML قديم و Astro لنفس URL.
[ ] صفر روابط داخلية 404 في نطاق المرحلة. صفر روابط .html جديدة. صفر noindex جديد.
[ ] لا تغيير غير موثّق في H1/title/description/canonical.
[ ] تقرير المرحلة أُنشئ بالبنية: Summary / Files changed / Routes affected / SEO before-after / Build result / Risks found / Fixes applied / Remaining recommendations / Next prompt readiness.
إذا فشل أي شرط: أصلحه داخل نفس المرحلة. ممنوع الانتقال للمرحلة التالية قبل اجتياز كل الشروط.


البرومبت 11 — Blog + Hub + Authors

[الصق GUARD RULES فوق]

المرحلة 11 — Scope: src/content/blog/* + src/pages/blog/* + src/pages/hub/* + src/pages/authors/* + RSS. لا تلمس docs/solutions/kernel/legal إلا روابط داخلية. لا تلمس Header/Footer العام.

Blog:
- src/content/blog/ + schema (title, description, canonical, pubDate, updatedDate, author, tags[], category, ogImage, draft:false).
- src/pages/blog/index.astro (قائمة + تصفية بالفئة، ترقيم إن لزم) + src/pages/blog/[...slug].astro.
- انقل كل المقالات الموجودة في sitemap حرفياً، ومنها على الأقل: ai-governance-saudi-arabia, pdpl-ai-compliance-guide, pdpl-ai-safety, pdpl-and-ai-saudi, nca-ecc-ai-controls-guide, iso-42001-saudi-implementation-guide, healthcare-ai-governance-saudi-hospitals, banking-ai-governance-sama-requirements, shadow-ai-discovery-saudi-company, vision-2030-ai-governance-roadmap, best-ai-governance-platforms-2026, ai-red-teaming-security-testing, hidden-ai-risks-saudi-organizations, what-is-ai-governance-saudi-companies, ai-governance-vs-ai-safety-vs-ai-security, ai-governance, ai-ethics-saudi-responsible-ai, ai-customer-data-protection-saudi, ai-audit-trail-saudi, ai-audit-trail-compliance-path, ai-firewall-why-you-need-it, sdaia-generative-ai-guidelines-practical-compliance — وأي مقال آخر في sitemap. لا تحذف/تختصر أي مقال. اعتمد sitemap كقائمة نهائية.
- RSS: أعد توليد /blog/feed.xml عبر @astrojs/rss بنفس البنية، تحقق أنه valid RSS ومسار feed.xml مطابق للقديم.
- JSON-LD لكل مقال: BlogPosting (headline, datePublished, dateModified, author, image) + BreadcrumbList. RelatedPosts (بالـ tags/category).

Authors: /authors/nasser-alabdullah/ — صفحة مؤلف (Person schema: name, url) تسرد مقالاته. لا تخترع مؤلفين غير موجودين.

Hub: /hub/ , /hub/ai-governance/ , /hub/compliance/ , /hub/solutions/ , /hub/use-cases/ — صفحات topical authority تربط المقالات/الحلول/الوثائق ذات الصلة. CollectionPage JSON-LD. انقل المحتوى الحالي حرفياً.

تحقق: كل المقالات 200، feed.xml صالح، /hub/* و /authors/* 200 في preview. نفّذ Gate.

تحسينات مقترحة: اربط كل مقال بالحل/الوثيقة المقابلة (blog ↔ solution ↔ doc) لإغلاق حلقة الـ internal linking. أضف "آخر تحديث" مرئياً لتعزيز الثقة والطزاجة. اجعل صفحات hub تعمل كـ pillar pages تشير لكل المحتوى الفرعي.

مخاطر يجب الانتباه لها: خطر فقدان مقال إذا اعتمد الوكيل على ذاكرته بدل sitemap — اشترط أن يطابق عدد المقالات المنقولة عدد ما في sitemap بالضبط ويُوثّق الفرق. خطر feed.xml: أي تغيير في المسار يكسر مشتركي RSS وقواعد robots — حافظ على المسار حرفياً.

ملفات متوقعة تتأثر: src/content/blog/, src/content/config.ts, src/pages/blog/, src/pages/hub/, src/pages/authors/, src/pages/blog/feed.xml.ts, RelatedLinks.astro.

معايير قبول المرحلة: عدد المقالات = sitemap، كل المقالات + 5 hub + author = 200، feed.xml صالح ومطابق المسار، BlogPosting+BreadcrumbList+Person+CollectionPage صالح، Gate مكتمل.

اسم التقرير: BLOG-HUB-MIGRATION-REPORT.md

البرومبت 12 — Legal + English

[الصق GUARD RULES فوق]

المرحلة 12 — Scope: الصفحات القانونية + الإنجليزية الموجودة فعلياً فقط + EnglishLayout + ضبط hreflang. لا تلمس أي نطاق آخر إلا روابط ضرورية.

تحذير حاسم: لا تنشئ أي صفحة إنجليزية غير موجودة في ROUTE-INVENTORY.md/sitemap. النسخة الإنجليزية المؤكدة قانونية فقط. لا تخترع /en/ رئيسية ولا /en/about/ ولا /en/solutions/ ولا /en/kernel/.

القانونية (عربي، dir=rtl): /privacy-policy/ , /privacy-cookies/ , /cookie-policy/ , /terms/ , /pdpl-statement/ , /data-processing-agreement/
الإنجليزية المؤكدة (dir=ltr، lang=en): /en/privacy-policy/ , /en/cookie-policy/ , /en/terms/ , /en/pdpl-statement/ , /en/data-processing-agreement/
ملاحظة /en/ الرئيسية: إن وُجدت في ROUTE-INVENTORY.md فانقلها كما هي؛ إن لم تُؤكَّد، لا تنشئها — سجّل ذلك في التقرير.

لكل صفحة:
- محتوى قانوني حرفي كما هو (مستندات لا تُعاد صياغتها إطلاقاً). H1 واحد. canonical self-reference.
- JSON-LD: WebPage/LegalDocument. أضف "ليس استشارة قانونية" حيث مناسب إن كان في الأصل.
- EnglishLayout: lang=en, dir=ltr (الإنجليزية). العربية ArabicLayout.

hreflang الصارم (الأزواج الموجودة فعلياً فقط):
- العربية: hreflang="ar-SA" href=الرابط العربي + hreflang="en-SA" href=النظير الإنجليزي (إن وُجد فقط).
- الإنجليزية: العكس متبادل.
- x-default → النسخة العربية.
- ممنوع ربط نظير إنجليزي غير موجود. الصفحات العربية بلا نظير إنجليزي: hreflang ذاتي عربي + x-default فقط.
- مبدّل اللغة ينقل بين الأزواج الموجودة فقط؛ يُخفى/يُعطّل حيث لا نظير.

تحقق: تطابق hreflang ثنائي الاتجاه (ar↔en) للأزواج فقط، كل الصفحات 200. نفّذ Gate.

تحسينات مقترحة: أنشئ خريطة أزواج لغوية واحدة في src/data/i18n-pairs.ts تُولّد hreflang تلقائياً وتمنع الأخطاء اليدوية. تحقق برمجياً أن كل hreflang له نظير متبادل قبل البناء.

مخاطر يجب الانتباه لها: أخطر خطأ هو hreflang أحادي (صفحة تشير لنظير لا يشير لها) → Google يتجاهله. ثاني خطر: ربط نظير وهمي → 404 في hreflang. ثالث: تغيير صياغة مستند قانوني = مسؤولية قانونية، ممنوع منعاً باتاً.

ملفات متوقعة تتأثر: src/pages/privacy-policy/, cookie-policy/, privacy-cookies/, terms/, pdpl-statement/, data-processing-agreement/, src/pages/en/**, src/layouts/EnglishLayout.astro, src/data/i18n-pairs.ts, SEOHead.astro.

معايير قبول المرحلة: كل القانونية + الإنجليزية المؤكدة 200، hreflang متبادل صحيح للأزواج فقط، صفر hreflang وهمي، محتوى قانوني حرفي، Gate مكتمل.

اسم التقرير: LEGAL-EN-MIGRATION-REPORT.md

البرومبت 13 — Legacy Cleanup الآمن

[الصق GUARD RULES فوق]

المرحلة 13 — Cleanup آمن. ممنوع delete مباشر. الترتيب الإجباري:
Inventory → Mapping → Quarantine → Build → QA → Delete (commit ثانٍ منفصل).

شرط مسبق: نجاح كل المراحل 5–12 وبناء dist يحوي كل الصفحات. أنشئ branch: cleanup/remove-legacy-html. كل خطوة في commit منفصل.

الخطوة 1 — Inventory (لا حذف):
احصر كل *.html قديم (خارج إخراج Astro)، CSS/JS قديمة غير مرتبطة بـ Astro، أصول يتيمة، أي build/index قديم.
أنشئ LEGACY-CLEANUP-INVENTORY.md:
| الملف | النوع | يخدم أي URL؟ | له نظير Astro 200؟ | مرجوع في أي src/؟ | حالة | السبب |

الخطوة 2 — Mapping (بوابة أمان):
لكل HTML قديم: أثبت وجود route Astro مقابل يُرجع 200 ومحتواه مكافئ (قارن H1/title/canonical/جوهر المحتوى).
إن لم يوجد نظير مؤكد → "DO NOT DELETE — needs migration"، أبلغ وتوقف عن حذفه.
تحقق أن الملفات الخاصة محفوظة ومنقولة إلى public/: robots.txt, sitemap.xml, blog/feed.xml, llms.txt, llms-full.txt, ai.txt, manifest.webmanifest, 404.html, 500.html, favicon, og-images. هذه لا تُحذف أبداً.

الخطوة 3 — Quarantine (لا حذف بعد):
انقل الملفات المؤكدة "آمنة + لها نظير 200" إلى مجلد _legacy_quarantine/ خارج أي خدمة أو بناء (أضفه لـ .gitignore الخدمة، أو استبعده من النشر). لا تحذف من git بعد. هذا commit مستقل.

الخطوة 4 — Build:
npm run build ثم npm run preview. تأكد أن إخراج البناء لا يعتمد على أي ملف منقول للحجر، وأن كل المسارات الـ110+ تُرجع 200، والملفات الخاصة موجودة في dist.

الخطوة 5 — QA:
افحص: صفر ازدواج خدمة، صفر مرجع متبقٍ لملفات .html محذوفة داخل src/ (أصلحه للرابط النظيف)، .gitignore يتجاهل dist/ و node_modules/. صفر روابط داخلية مكسورة.

الخطوة 6 — Delete (commit ثانٍ منفصل فقط بعد نجاح QA):
احذف نهائياً فقط محتويات _legacy_quarantine/ المؤكدة. لا تحذف: أي محتوى لم يُهاجَر، الملفات الخاصة، .git، إعدادات النشر، render.yaml.

ممنوع لمس render.yaml في هذه المرحلة. أنشئ LEGACY-CLEANUP-REPORT.md (ما حُجِر/ما حُذف/ما حُفظ ولماذا/يحتاج مراجعة يدوية/نتيجة build وQA). لا تدمج الـbranch قبل مراجعة بشرية. نفّذ Gate.

تحسينات مقترحة: قبل الحجر، خذ لقطة قائمة كل URLs الحية من sitemap وقارنها بإخراج dist بعد الحجر — أي URL ينقص = ملف حُجِر خطأً. أرفق هذه المقارنة في التقرير كدليل أمان.

مخاطر يجب الانتباه لها: أخطر سيناريو حذف ملف يخدم مساراً لم يُهاجَر فعلياً (الوكيل ظنّ النظير موجوداً). الحجر بدل الحذف يحميك تماماً هنا. خطر ثانٍ: حذف ملف خاص (llms.txt/ai.txt/feed.xml) عن طريق تصنيفه "قديم". خطر ثالث: تعديل .gitignore بشكل يستبعد ملفات public ضرورية.

ملفات متوقعة تتأثر: ملفات HTML قديمة (إلى _legacy_quarantine/ ثم حذف)، .gitignore، public/ (نقل الملفات الخاصة)، مراجع داخلية متبقية في src/.

معايير قبول المرحلة: كل المسارات الـ110+ في dist 200 بعد الحجر، صفر ملف خاص محذوف، صفر ازدواج خدمة، حذف في commit ثانٍ فقط، render.yaml غير ممسوس، Gate مكتمل.

أسماء التقارير: LEGACY-CLEANUP-INVENTORY.md + LEGACY-CLEANUP-REPORT.md

البرومبت 14 — SEO Final + Render + QA

[الصق GUARD RULES فوق]

المرحلة 14 — الفحص الشامل النهائي قبل النشر + تعديل Render محدود ومشروط. أصلح كل ما تجده فعلياً.

SEO/QA الشامل — تحقق من:
1. كل المسارات الـ110+ (من sitemap الأصلي) تُرجع 200 في preview. صفر 404 داخلي.
2. صفر روابط .html في التنقل. كل الروابط trailing slash.
3. H1 واحد لكل صفحة. title + meta description لكل صفحة.
4. canonical self-reference (non-www, https) مطابق للرابط.
5. لا noindex على أي صفحة عامة.
6. sitemap.xml يحوي كل الصفحات الجديدة (طابقه بالأصلي عنصراً عنصراً). blog/feed.xml صالح. robots.txt + llms.txt + llms-full.txt + ai.txt + manifest.webmanifest + 404.html + 500.html موجودة في الإخراج.
7. hreflang ar-SA/en-SA متبادل للأزواج الموجودة فقط + x-default. صفر hreflang وهمي.
8. JSON-LD صالح ومطابق للمحتوى المرئي: Organization+SoftwareApplication+FAQPage (الرئيسية)، Service+BreadcrumbList (solutions، areaServed للمحلية)، SoftwareApplication (kernel index)، TechArticle (docs)، BlogPosting+Person+CollectionPage (blog/authors/hub)، LegalDocument (القانونية).
9. Google tag G-8LLESL207Q في كل الصفحات.
10. صفر صفحات orphan؛ كل صفحة لها رابط داخلي وارد.
11. المحتوى الأساسي يظهر بدون JavaScript (اختبر بتعطيل JS).
12. صفر horizontal overflow على 360/390/430px.
13. CWV: صفر CLS من الأنيميشن، صور بـ width/height، GSAP يُحمَّل فقط في الرئيسية + /kernel/.

Render — تعديل محدود ومشروط (brightai-site فقط):
- ممنوع استبدال render.yaml بالكامل. ممنوع لمس brightai-api، /api/، /ws/، envVars، healthCheckPath.
- لا تغيّر publishPath إلى dist إلا بعد إثبات (مكتوب في التقرير) أن Astro dist يحوي كل المسارات الـ110+ تُرجع 200 محلياً والملفات الخاصة موجودة.
- إن لم يكتمل الإثبات: لا تغيّر render.yaml إطلاقاً وسجّل "Render switch deferred".
- إن اكتمل: عدّل buildCommand/publishPath لخدمة brightai-site فقط (npm install && npm run build، publish dist)، وحافظ على rewrites /api/* و /ws/* كما هي حرفياً، وعلى non-www + https.
- redirects: أي .html قديم لا يزال مفهرساً → 301 للرابط النظيف، دون كسر قواعد API.

أنشئ SEO-MIGRATION-CHECK.md (جدول: Page|Status|SEO|Mobile|Schema|Fix) ثم أصلح فعلياً. وأنشئ FINAL-QA-REPORT.md + RENDER-DEPLOYMENT-CHECK.md. شغّل npm run build و npm run preview. نفّذ Gate.

تحسينات مقترحة: أنشئ سكربت تحقق scripts/verify-routes.mjs يقرأ sitemap.xml ويتأكد أن كل URL له مقابل في dist يُرجع 200 — يصبح اختبار انحدار دائم قبل كل نشر. أضف فحص JSON-LD آلي.

مخاطر يجب الانتباه لها: أخطر خطأ في كامل المشروع هو تبديل publishPath إلى dist قبل اكتمال الترحيل = مئات 404 لحظياً. شرط الإثبات المكتوب يحمي من هذا. خطر ثانٍ: تعديل عام على render.yaml يكسر brightai-api. خطر ثالث: redirects تتعارض مع /api/*.

ملفات متوقعة تتأثر: render.yaml (brightai-site فقط، مشروط)، scripts/verify-routes.mjs، تقارير، وأي إصلاحات SEO نقطية عبر النطاقات.

معايير قبول المرحلة: 110+ مسار 200، صفر 404/orphan/overflow، كل schema صالح، الملفات الخاصة موجودة، render.yaml إما غير ممسوس أو معدّل بشروط موثّقة دون كسر API، Gate مكتمل.

أسماء التقارير: SEO-MIGRATION-CHECK.md + FINAL-QA-REPORT.md + RENDER-DEPLOYMENT-CHECK.md

البرومبتات الإضافية الاختيارية (بعد البرومبت 14)

A — View Transitions

[الصق GUARD RULES فوق]

بعد استقرار الترحيل والنشر فقط. فعّل Astro View Transitions دون كسر SEO أو تراكم GSAP.
- أضف  في BaseLayout .
- transition:name لعناصر مشتركة (Header logo، hero heading) لاستمرارية هادئة.
- أعد تهيئة سكربتات DOM عند astro:page-load، ونظّف ScrollTrigger وGSAP عند astro:before-swap (امنع تسرب الذاكرة وتراكم triggers).
- كل صفحة تبقى static HTML مفهرسة كاملة (View Transitions تحسين بصري فقط).
- عطّل التحريك الانتقالي عند prefers-reduced-motion.
- اختبر تنقل الرئيسية→solutions→kernel→blog→docs، وتأكد أن GSAP لا يُحمّل مرتين ولا يتراكم.
Gate + VIEW-TRANSITIONS-REPORT.md.
مخاطر: تراكم ScrollTrigger عند كل تنقل (تسرب ذاكرة) إن لم يُنظّف عند before-swap. ملفات تتأثر: BaseLayout.astro, home-motion.ts, kernel-map.ts.

B — Performance + Images

[الصق GUARD RULES فوق]

حسّن الأداء دون تغيير محتوى/تصميم.
- حوّل الصور إلى astro:assets  بصيغ WebP/AVIF، loading=lazy، width/height صريحة، decoding=async. صور الطية العليا: eager + fetchpriority=high.
- الخطوط: self-host woff2 أو preload+preconnect، font-display:swap، subset عربي/لاتيني.
- critical CSS للطية العليا inline، الباقي مؤجّل. GSAP عبر dynamic import فقط (لا يدخل bundle الصفحات الأخرى).
- احذف CSS/JS غير المستخدم، فعّل ضغط البناء.
- الهدف: Performance 95+، CLS≈0، LCP<2.5s على محاكاة موبايل بطيء.
Gate + PERFORMANCE-REPORT.md (حجم JS لكل صفحة قبل/بعد).
مخاطر: تحويل صور قديمة قد يكسر مساراتها إن كانت مرجعية في HTML قديم محجور. ملفات تتأثر: كل صفحة بصور، astro.config, الخطوط، home-motion.ts.

C — Accessibility WCAG AA

[الصق GUARD RULES فوق]

ارفع a11y إلى WCAG 2.1 AA دون تغيير الهوية البصرية.
- تباين AA لكل نص (راجع --muted فوق --bg خصوصاً). focus-visible واضح. skip-to-content link.
- aria-label/aria-expanded للقوائم/dropdowns/accordion FAQ. keyboard nav كامل (Tab/Esc/أسهم).
- alt وصفي للصور المعلوماتية، alt="" للزخرفية. landmarks صحيحة (header/nav/main/footer). touch ≥44px.
- لا معلومة تُنقل باللون/الحركة وحدها.
- اختبر قارئ شاشة على الرئيسية + صفحة حل + صفحة kernel + مقال.
Gate + A11Y-REPORT.md (معايير + إصلاحات).
مخاطر: تعديل ألوان للتباين قد يمسّ الهوية — عدّل القيم في tokens.css فقط وبحد أدنى. ملفات تتأثر: tokens.css, Header.astro, MobileNav.astro, FAQAccordion.astro.

D — JSON-LD + GEO/AEO

[الصق GUARD RULES فوق]

عزّز الظهور في محركات الذكاء (AEO/GEO) بلا حشو وبلا claims غير مرئية.
- وسّع Organization: logo, sameAs, contactPoint (واتساب), address (السعودية), areaServed.
- SoftwareApplication مفصّل لـ BrightAI Kernel (applicationCategory, featureList, offers "تواصل للسعر").
- FAQPage يطابق أسئلة الرئيسية حرفياً. HowTo لوثائق الخطوات فقط.
- حدّث llms.txt + llms-full.txt + ai.txt لتعكس المسارات/الأقسام الجديدة بدقة (لا تحذفها).
- TL;DR أعلى الوثائق الطويلة (تلخيص أمين فقط).
- تحقق أن كل schema صالح ومطابق للمحتوى المرئي. صياغة آمنة (يدعم الجاهزية، ليس استشارة قانونية).
Gate + STRUCTURED-DATA-REPORT.md.
مخاطر: schema لمحتوى غير موجود = مخالفة Google. ملفات تتأثر: SEOHead.astro, ملفات schema، public/llms*.txt, public/ai.txt.

E — Safe Future Change Prompt (قالب عام لأي تعديل لاحق)

[الصق GUARD RULES فوق]

برومبت تعديل آمن عام لأي تغيير مستقبلي.
قبل: حدّد بدقة الملف/القسم المستهدف فقط (Scope Lock). خذ SEO Snapshot للصفحة المستهدفة.
أثناء: لا تغيّر path/H1/canonical/meta أو أقسام أخرى. حافظ على RTL/LTR وreduced-motion. لا تلمس render.yaml/API/الملفات الخاصة.
بعد: npm run build + preview، تأكد الصفحة المستهدفة 200 ولا صفحة أخرى تأثرت (قارن Snapshot)، اختبر 390px.
سلّم diff واضح + سطر يصف ما تغيّر ولماذا. لا TODO معلّق. Gate مصغّر.

[اكتب هنا التعديل المطلوب: الصفحة + القسم + التغيير المحدد]
مخاطر: التعديلات الصغيرة المتكررة تتراكم وتكسر اتساق الـ Design System — وثّق كل تغيير. ملفات تتأثر: حسب التعديل (نطاق مقفل).

خلاصة صارمة: أهم ثلاث بوابات في كل هذا هي (1) عدم تبديل publishPath قبل إثبات مكتوب أن dist كامل، (2) Quarantine قبل Delete، (3) hreflang للأزواج الموجودة فعلياً فقط. لو حافظت على هذه الثلاث، يصعب جداً أن ينكسر SEO أو Render. ابدأ بالبرومبت 8 الآن، ولا تتجاوز أي Gate.