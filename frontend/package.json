{
  "name": "brightai-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "dev:frontend": "node scripts/dev-server.mjs",
    "test": "vitest run --config vitest.config.mjs && node --test \"tests/**/*.test.mjs\"",
    "test:watch": "vitest --config vitest.config.mjs",
    "test:ai-gateway:mock": "AI_GATEWAY_MOCK_MODE=1 node scripts/test-gemini-unified-api.mjs",
    "test:ai-gateway:live": "node scripts/test-gemini-unified-api.mjs --live",
    "lint": "eslint server.js routes/**/*.js services/**/*.js utils/**/*.js middleware/**/*.js --max-warnings=0",
    "validate:env": "node -e \"if (!process.env.DATABASE_URL && process.env.AI_GATEWAY_MOCK_MODE !== '1') { console.warn('⚠️ Warning: DATABASE_URL is missing. Please configure it in Render or a local .env file.'); } else { console.log('✅ Environment check passed.'); }\""
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.22.1",
    "pg": "^8.21.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
